<UserControl
    x:Class="RCabinet.Views.MappingCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RCabinet.Views"
    xmlns:mahapps="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:RCabinet.Models"
    xmlns:viewModels="clr-namespace:RCabinet.ViewModels"
    Name="MappingCardUserControl"
    d:DataContext="{d:DesignInstance Type={x:Type viewModels:ExchangeViewModel}}"
    d:DesignHeight="900"
    d:DesignWidth="1440"
    Background="{StaticResource color6}"
    Loaded="MappingCardUserControl_Loaded"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <!--  Solution for hiding datagrid column with binding from https://stackoverflow.com/a/7711611  -->
        <FrameworkElement x:Key="ProxyElement" DataContext="{Binding}" />
    </UserControl.Resources>
    <Grid Margin="-1,0,1,9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ContentControl Content="{StaticResource ProxyElement}" Visibility="Collapsed" />
        <Button
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="15,5,5,5"
            Padding="10"
            HorizontalAlignment="Left"
            Command="{Binding GoToMainMenu}"
            Content="Return to Main Menu"
            FontSize="13" />
        <Label
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Content="Exchange Card"
            FontSize="16"
            FontWeight="Bold" />
        <TextBox
            x:Name="txCardNO"
            Name="txtCardNo"
            Grid.Row="1"
            Grid.Column="0"
            Width="262"
            Margin="84,6,0,6"
            HorizontalAlignment="Left"
            VerticalContentAlignment="Top"
            mahapps:TextBoxHelper.ClearTextButton="True"
            mahapps:TextBoxHelper.Watermark="Search items..."
            KeyDown="txtCardNo_KeyDown"
            Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
        <DataGrid
            Name="ItemsGrid"
            Grid.Row="2"
            Grid.Column="0"
            Margin="0,239,452,15"
            d:DataContext="{d:DesignInstance Type={x:Type models:InventoryItem}}"
            AutoGenerateColumns="False"
            Background="#33DB80F0"
            BorderBrush="Gray"
            BorderThickness="1"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            GridLinesVisibility="All"
            IsReadOnly="True"
            ItemsSource="{Binding FilteredItems}"
            RowHeaderWidth="0"
            SelectedIndex="{Binding SelectedIndex}"
            SelectedItem="{Binding SelectedItem}"
            SelectionMode="Single"
            SelectionUnit="FullRow">
            <DataGrid.Style>
                <Style BasedOn="{StaticResource MahApps.Styles.DataGrid}" TargetType="{x:Type DataGrid}">
                    <Setter Property="AlternatingRowBackground" Value="{DynamicResource GrayBrush10}" />
                </Style>
            </DataGrid.Style>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="400"
                    Binding="{Binding EPC}"
                    Header="EPC"
                    SortDirection="{x:Null}" />
                <DataGridTextColumn
                    Width="200"
                    Binding="{Binding DateTime}"
                    Header="DateTime" />
                <DataGridTextColumn
                    Width="200"
                    Binding="{Binding User}"
                    Header="User" />

            </DataGrid.Columns>
        </DataGrid>
        <StackPanel
            Grid.RowSpan="3"
            Margin="997,49,20,0"
            Orientation="Vertical">
            <Button
                Name="btReadingEPC"
                Width="166"
                Height="40"
                Margin="10,10,10,10"
                Padding="10,10,10,10"
                Click="btReadingEPC_Click"
                Command="{Binding MoveToAddItemScreen}"
                Content="Start Reading EPC Tags"
                FontSize="13"
                Visibility="{Binding CurrentUser.Permissions.CanAddEditItems, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <DataGrid
                x:Name="ItemsGrid_Copy"
                Width="435"
                Height="784"
                d:DataContext="{d:DesignInstance Type={x:Type models:InventoryItem}}"
                AutoGenerateColumns="False"
                Background="#33DB80F0"
                BorderBrush="Gray"
                BorderThickness="1"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                GridLinesVisibility="All"
                IsReadOnly="True"
                ItemsSource="{Binding FilteredItems}"
                RowHeaderWidth="0"
                SelectedIndex="{Binding SelectedIndex}"
                SelectedItem="{Binding SelectedItem}"
                SelectionMode="Single"
                SelectionUnit="FullRow">
                <DataGrid.Style>
                    <Style BasedOn="{StaticResource MahApps.Styles.DataGrid}" TargetType="{x:Type DataGrid}">
                        <Setter Property="AlternatingRowBackground" Value="{DynamicResource GrayBrush10}" />
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="200"
                        Binding="{Binding MO}"
                        Header="Mo"
                        SortDirection="{x:Null}" />
                    <DataGridTextColumn
                        Width="200"
                        Binding="{Binding CardID}"
                        Header="Card ID" />

                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <Label
            Grid.Row="1"
            Margin="20,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Content="Card ID"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="19,12,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Style No:"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="303,12,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Sale Order (SO):"
            Style="{StaticResource LabelUI}" />

        <Label
            Grid.Row="2"
            Margin="422,12,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />

        <Label
            Grid.Row="2"
            Margin="601,12,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Order Number (MO):"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="121,12,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />


        <Label
            Grid.Row="2"
            Margin="755,12,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="19,50,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Workstate :"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="303,50,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Color:"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="422,50,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="121,50,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="19,90,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Size:"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="303,90,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Bundle No:"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="422,90,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="121,90,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="19,130,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Join Qty"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="303,130,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Available Qty:"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="422,130,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="121,130,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="604,90,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Quantity:"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="754,90,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <Label
            Grid.Row="2"
            Margin="602,132,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Country:"
            Style="{StaticResource LabelUI}" />
        <Label
            Grid.Row="2"
            Margin="753,132,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="___"
            Style="{StaticResource LabelUIWhite}" />
        <ComboBox
            Name="cbComPort"
            Grid.Row="1"
            Width="168"
            Margin="436,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center" />


    </Grid>
</UserControl>
